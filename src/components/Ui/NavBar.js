import styles from "./NavBar.module.css";

const NavBar = () => {
  return (
    <div className={styles.wrapper}>
      <div>GROUPS</div>
      <div>EMPLOYEES</div>
    </div>
  );
};

export default NavBar;
